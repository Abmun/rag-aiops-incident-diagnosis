"""
src/api/routes.py
───────────────────
Currently unused: all endpoints are defined directly on the `app` object in
`src/api/main.py`. This module is a placeholder for splitting routes into an
`APIRouter` (e.g. `create_router()`) if/when the API surface grows enough to
warrant it.
"""
